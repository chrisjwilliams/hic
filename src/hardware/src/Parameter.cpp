#include "Parameter.h"


namespace hic {


/**
 * @details Constructs a Parameter object.
template<typename T>
Parameter<T>::Parameter(typeT type, const QString& name, const QString doc)
    : _type(type), _name(name), _doc(doc)
{
}
 */

/**
 * @details Destroys the Parameter object.
template<typename T>
Parameter<T>::~Parameter()
{
}
 */

} // namespace hic
